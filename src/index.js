import './style.css';
import Food from './assets/food.png';
import homePage from 'sr./mainpage.js';

  // Add the image to our existing div.
  const myIcon = new Image();
  myIcon.src = Food;

  element.appendChild(myIcon);

mainPage();